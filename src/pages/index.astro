---
// src/pages/index.astro - VERSIÓN MEJORADA
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

// Datos para el carrusel hero
const heroSlides = [
  {
    id: 1,
    titulo: "Transformamos Ideas en Soluciones Digitales",
    subtitulo: "Desarrollo de software empresarial de vanguardia",
    descripcion: "Llevamos tu negocio al siguiente nivel con tecnología de última generación y un equipo experto comprometido con tu éxito.",
    imagen: "/images/hero/slide1.jpg",
    cta: "Iniciar mi proyecto",
    link: "/contacto",
    stats: ["50+ Proyectos", "95% Satisfacción", "24/7 Soporte"]
  },
  {
    id: 2,
    titulo: "Innovación Que Impulsa Resultados",
    subtitulo: "Consultoría estratégica y desarrollo tecnológico",
    descripcion: "Más de 5 años ayudando a empresas líderes a optimizar procesos, reducir costos y aumentar su competitividad en el mercado digital.",
    imagen: "/images/hero/slide2.jpg",
    cta: "Ver casos de éxito",
    link: "/clientes",
    stats: ["15 Países", "$500M+ Facturación", "Industry 4.0"]
  },
  {
    id: 3,
    titulo: "Tu Socio Tecnológico de Confianza",
    subtitulo: "Soluciones integrales para empresas modernas",
    descripcion: "Desde sistemas ERP hasta aplicaciones móviles, creamos la tecnología que tu empresa necesita para liderar en su industria.",
    imagen: "/images/hero/slide3.jpg",
    cta: "Explorar productos",
    link: "/productos",
    stats: ["99.9% Uptime", "AI + IoT", "Cloud Native"]
  }
];

// Servicios destacados
const serviciosDestacados = [
  {
    icono: "🚀",
    titulo: "Desarrollo de Software",
    descripcion: "Aplicaciones web y móviles personalizadas con tecnología de vanguardia",
    caracteristicas: ["React/Vue/Angular", "Node.js/Python", "Cloud Native", "Microservicios"],
    precio: "Desde $2,000",
    popular: true
  },
  {
    icono: "🎯",
    titulo: "Consultoría Empresarial",
    descripcion: "Estrategias digitales que optimizan procesos y aumentan la productividad",
    caracteristicas: ["Análisis de procesos", "Digital Transformation", "Change Management", "ROI tracking"],
    precio: "Desde $500"
  },
  {
    icono: "🤖",
    titulo: "Inteligencia Artificial",
    descripcion: "Chatbots inteligentes y automatización que revolucionan tu negocio",
    caracteristicas: ["Machine Learning", "NLP", "Computer Vision", "Predictive Analytics"],
    precio: "Desde $800",
    nuevo: true
  },
  {
    icono: "☁️",
    titulo: "Soluciones Cloud",
    descripcion: "Migración e infraestructura en la nube para máxima escalabilidad",
    caracteristicas: ["AWS/Azure/GCP", "DevOps", "Kubernetes", "Monitoring 24/7"],
    precio: "Desde $1,200"
  }
];

// Clientes destacados para el carrusel
const clientesDestacados = [
  {
    nombre: "Corporación ABC",
    logo: "/images/clientes/abc-corp-logo.png",
    industria: "Manufactura",
    proyecto: "Sistema ERP Industrial",
    resultado: "40% reducción en tiempos de producción",
    testimonio: "MiEmpresa transformó completamente nuestros procesos industriales."
  },
  {
    nombre: "TechSolutions Inc.",
    logo: "/images/clientes/techsolutions-logo.png",
    industria: "Fintech",
    proyecto: "Plataforma de Pagos",
    resultado: "1M+ usuarios en 6 meses",
    testimonio: "La plataforma desarrollada nos permitió expandirnos internacionalmente."
  },
  {
    nombre: "RetailMax",
    logo: "/images/clientes/retailmax-logo.png",
    industria: "Retail",
    proyecto: "E-commerce Omnicanal",
    resultado: "200% crecimiento en ventas online",
    testimonio: "El e-commerce que crearon superó todas nuestras expectativas de ventas."
  }
];

// Números impactantes
const estadisticas = [
  { numero: "50+", texto: "Proyectos Completados", icono: "📊", descripcion: "En múltiples industrias" },
  { numero: "95%", texto: "Clientes Satisfechos", icono: "⭐", descripcion: "Calificación promedio" },
  { numero: "15", texto: "Países con Presencia", icono: "🌍", descripcion: "Alcance internacional" },
  { numero: "24/7", texto: "Soporte Técnico", icono: "🛡️", descripcion: "Siempre disponibles" }
];
---

<Layout title="Inicio - MiEmpresa | Transformación Digital Empresarial">
  <Header />
  
  <main>
    <!-- Hero Carousel Section -->
    <section class="hero-carousel-section">
      <div class="hero-carousel-container">
        <div class="hero-carousel" id="heroCarousel">
          {heroSlides.map((slide, index) => (
            <div class={`hero-slide ${index === 0 ? 'active' : ''}`} data-slide={index}>
              <div class="hero-background">
                <img src={slide.imagen} alt={slide.titulo} />
                <div class="hero-overlay"></div>
              </div>
              
              <div class="hero-content">
                <div class="container">
                  <div class="hero-text">
                    <h1 class="hero-title">{slide.titulo}</h1>
                    <h2 class="hero-subtitle">{slide.subtitulo}</h2>
                    <p class="hero-description">{slide.descripcion}</p>
                    
                    <div class="hero-stats">
                      {slide.stats.map((stat) => (
                        <div class="hero-stat-item">
                          <span class="stat-value">{stat}</span>
                        </div>
                      ))}
                    </div>
                    
                    <div class="hero-actions">
                      <a href={slide.link} class="btn-primary hero-cta">
                        {slide.cta}
                      </a>
                      <button class="btn-secondary play-video" data-video="company-intro">
                        ▶️ Ver video corporativo
                      </button>
                    </div>
                  </div>
                </div>
              </div>
              
              <!-- Animated Elements -->
              <div class="floating-elements">
                <div class="floating-circle circle-1"></div>
                <div class="floating-circle circle-2"></div>
                <div class="floating-circle circle-3"></div>
              </div>
            </div>
          ))}
        </div>
        
        <!-- Carousel Controls -->
        <div class="hero-carousel-controls">
          <button class="carousel-btn prev" id="heroPrevBtn">❮</button>
          <button class="carousel-btn next" id="heroNextBtn">❯</button>
        </div>
        
        <!-- Carousel Indicators -->
        <div class="hero-carousel-indicators">
          {heroSlides.map((_, index) => (
            <button class={`indicator ${index === 0 ? 'active' : ''}`} data-slide={index}></button>
          ))}
        </div>
        
        <!-- Scroll Indicator -->
        <div class="scroll-indicator">
          <div class="scroll-text">Descubre más</div>
          <div class="scroll-arrow">↓</div>
        </div>
      </div>
    </section>

    <!-- Animated Stats Section -->
    <section class="stats-section">
      <div class="container">
        <div class="stats-grid">
          {estadisticas.map((stat, index) => (
            <div class="stat-card" data-index={index}>
              <div class="stat-icon">{stat.icono}</div>
              <div class="stat-number" data-target={stat.numero.replace(/\D/g, '')}>{stat.numero}</div>
              <div class="stat-text">{stat.texto}</div>
              <div class="stat-description">{stat.descripcion}</div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Services Preview with Animations -->
    <section class="services-preview">
      <div class="container">
        <div class="section-header">
          <h2>Nuestros Servicios Destacados</h2>
          <p>Soluciones tecnológicas que impulsan el crecimiento de tu empresa</p>
        </div>
        
        <div class="services-grid">
          {serviciosDestacados.map((servicio, index) => (
            <div class="service-card" data-index={index}>
              {servicio.popular && (
                <div class="service-badge popular">🔥 Más Popular</div>
              )}
              {servicio.nuevo && (
                <div class="service-badge nuevo">✨ Nuevo</div>
              )}
              
              <div class="service-icon">{servicio.icono}</div>
              <h3>{servicio.titulo}</h3>
              <p class="service-description">{servicio.descripcion}</p>
              
              <ul class="service-features">
                {servicio.caracteristicas.map((caracteristica) => (
                  <li>{caracteristica}</li>
                ))}
              </ul>
              
              <div class="service-price">{servicio.precio}</div>
              
              <div class="service-actions">
                <button class="btn-primary service-cta" data-service={servicio.titulo}>
                  Solicitar información
                </button>
                <a href="/servicios" class="service-link">Ver detalles →</a>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Interactive Clients Carousel -->
    <section class="clients-carousel-section">
      <div class="container">
        <div class="section-header">
          <h2>Clientes que Confían en Nosotros</h2>
          <p>Historias de éxito que hablan por sí solas</p>
        </div>
        
        <div class="clients-carousel-container">
          <div class="clients-carousel" id="clientsCarousel">
            {clientesDestacados.map((cliente, index) => (
              <div class={`client-slide ${index === 0 ? 'active' : ''}`} data-slide={index}>
                <div class="client-content">
                  <div class="client-logo">
                    <img src={cliente.logo} alt={`Logo de ${cliente.nombre}`} />
                  </div>
                  
                  <div class="client-info">
                    <h3>{cliente.nombre}</h3>
                    <span class="client-industry">{cliente.industria}</span>
                  </div>
                  
                  <div class="client-project">
                    <h4>Proyecto: {cliente.proyecto}</h4>
                    <div class="client-result">
                      <span class="result-icon">📈</span>
                      <span class="result-text">{cliente.resultado}</span>
                    </div>
                  </div>
                  
                  <blockquote class="client-testimonial">
                    "{cliente.testimonio}"
                  </blockquote>
                  
                  <button class="btn-secondary case-study-btn" data-client={cliente.nombre}>
                    Ver caso completo
                  </button>
                </div>
              </div>
            ))}
          </div>
          
          <div class="clients-carousel-controls">
            <button class="carousel-btn prev" id="clientsPrevBtn">❮</button>
            <button class="carousel-btn next" id="clientsNextBtn">❯</button>
          </div>
        </div>
      </div>
    </section>

    <!-- Interactive Technology Stack -->
    <section class="technology-section">
      <div class="container">
        <div class="section-header">
          <h2>Tecnologías de Vanguardia</h2>
          <p>Utilizamos las herramientas más modernas del mercado</p>
        </div>
        
        <div class="tech-grid">
          <div class="tech-category" data-category="frontend">
            <h3>Frontend</h3>
            <div class="tech-icons">
              <div class="tech-icon" data-tech="React">⚛️</div>
              <div class="tech-icon" data-tech="Vue">💚</div>
              <div class="tech-icon" data-tech="Angular">🔺</div>
              <div class="tech-icon" data-tech="TypeScript">🔷</div>
            </div>
          </div>
          
          <div class="tech-category" data-category="backend">
            <h3>Backend</h3>
            <div class="tech-icons">
              <div class="tech-icon" data-tech="Node.js">💚</div>
              <div class="tech-icon" data-tech="Python">🐍</div>
              <div class="tech-icon" data-tech="Java">☕</div>
              <div class="tech-icon" data-tech="PostgreSQL">🐘</div>
            </div>
          </div>
          
          <div class="tech-category" data-category="cloud">
            <h3>Cloud & DevOps</h3>
            <div class="tech-icons">
              <div class="tech-icon" data-tech="AWS">☁️</div>
              <div class="tech-icon" data-tech="Docker">🐳</div>
              <div class="tech-icon" data-tech="Kubernetes">⚙️</div>
              <div class="tech-icon" data-tech="GitHub">🔗</div>
            </div>
          </div>
          
          <div class="tech-category" data-category="ai">
            <h3>AI & Data</h3>
            <div class="tech-icons">
              <div class="tech-icon" data-tech="TensorFlow">🧠</div>
              <div class="tech-icon" data-tech="OpenAI">🤖</div>
              <div class="tech-icon" data-tech="Analytics">📊</div>
              <div class="tech-icon" data-tech="IoT">🌐</div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Call to Action with Animation -->
    <section class="cta-section">
      <div class="cta-background"></div>
      <div class="container">
        <div class="cta-content">
          <h2>¿Listo para Transformar tu Empresa?</h2>
          <p>Únete a las empresas líderes que ya están revolucionando su industria con nuestras soluciones</p>
          
          <div class="cta-stats">
            <div class="cta-stat">
              <span class="cta-stat-number">2-4</span>
              <span class="cta-stat-text">semanas para empezar</span>
            </div>
            <div class="cta-stat">
              <span class="cta-stat-number">24/7</span>
              <span class="cta-stat-text">soporte incluido</span>
            </div>
            <div class="cta-stat">
              <span class="cta-stat-number">ROI</span>
              <span class="cta-stat-text">garantizado</span>
            </div>
          </div>
          
          <div class="cta-buttons">
            <a href="/contacto" class="btn-primary cta-primary">
              🚀 Iniciar mi proyecto ahora
            </a>
            <a href="/productos" class="btn-secondary cta-secondary">
              Ver nuestros productos
            </a>
          </div>
          
          <div class="cta-footer">
            <p>💬 <strong>Consulta gratuita</strong> • 📞 <strong>Respuesta en 24 horas</strong> • 🛡️ <strong>Sin compromiso</strong></p>
          </div>
        </div>
      </div>
    </section>

    <!-- Video Modal -->
    <div id="videoModal" class="video-modal">
      <div class="video-modal-content">
        <span class="video-modal-close">&times;</span>
        <div class="video-container">
          <iframe 
            id="videoFrame"
            src="" 
            frameborder="0" 
            allowfullscreen>
          </iframe>
        </div>
      </div>
    </div>
  </main>
  
  <Footer />
</Layout>

<style>
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  /* Hero Carousel */
  .hero-carousel-section {
    position: relative;
    height: 100vh;
    min-height: 600px;
    overflow: hidden;
  }

  .hero-carousel-container {
    position: relative;
    height: 100%;
  }

  .hero-carousel {
    height: 100%;
    position: relative;
  }

  .hero-slide {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
    transition: opacity 1.5s ease-in-out;
    display: flex;
    align-items: center;
  }

  .hero-slide.active {
    opacity: 1;
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
  }

  .hero-background img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: brightness(0.7);
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, rgba(37, 99, 235, 0.8) 0%, rgba(30, 64, 175, 0.6) 100%);
  }

  .hero-content {
    position: relative;
    z-index: 2;
    color: white;
    width: 100%;
  }

  .hero-text {
    max-width: 700px;
    animation: heroSlideIn 1s ease-out;
  }

  .hero-title {
    font-size: 4rem;
    font-weight: 800;
    margin-bottom: 1rem;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    line-height: 1.1;
  }

  .hero-subtitle {
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
    color: #fbbf24;
    text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
  }

  .hero-description {
    font-size: 1.2rem;
    margin-bottom: 2rem;
    opacity: 0.95;
    line-height: 1.6;
    text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
  }

  .hero-stats {
    display: flex;
    gap: 2rem;
    margin-bottom: 3rem;
    flex-wrap: wrap;
  }

  .hero-stat-item {
    background: rgba(255, 255, 255, 0.15);
    padding: 1rem 1.5rem;
    border-radius: 25px;
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
  }

  .stat-value {
    font-weight: 700;
    font-size: 1.1rem;
  }

  .hero-actions {
    display: flex;
    gap: 1.5rem;
    flex-wrap: wrap;
    align-items: center;
  }

  .btn-primary,
  .btn-secondary {
    padding: 1.2rem 2.5rem;
    border-radius: 50px;
    font-weight: 600;
    font-size: 1.1rem;
    cursor: pointer;
    transition: all 0.3s ease;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    border: none;
  }

  .btn-primary {
    background: linear-gradient(45deg, #2563eb, #3b82f6);
    color: white;
    box-shadow: 0 4px 15px rgba(37, 99, 235, 0.4);
  }

  .btn-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 25px rgba(37, 99, 235, 0.6);
    background: linear-gradient(45deg, #1d4ed8, #2563eb);
  }

  .btn-secondary {
    background: transparent;
    color: white;
    border: 2px solid white;
  }

  .btn-secondary:hover {
    background: white;
    color: #1e293b;
    transform: translateY(-2px);
  }

  /* Floating Elements */
  .floating-elements {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 1;
  }

  .floating-circle {
    position: absolute;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.1);
    animation: float 6s ease-in-out infinite;
  }

  .circle-1 {
    width: 100px;
    height: 100px;
    top: 20%;
    right: 10%;
    animation-delay: 0s;
  }

  .circle-2 {
    width: 150px;
    height: 150px;
    bottom: 30%;
    right: 20%;
    animation-delay: 2s;
  }

  .circle-3 {
    width: 80px;
    height: 80px;
    top: 60%;
    right: 5%;
    animation-delay: 4s;
  }

  /* Carousel Controls */
  .hero-carousel-controls {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 100%;
    display: flex;
    justify-content: space-between;
    padding: 0 3rem;
    pointer-events: none;
    z-index: 3;
  }

  .carousel-btn {
    background: rgba(255, 255, 255, 0.2);
    color: white;
    border: none;
    width: 4rem;
    height: 4rem;
    border-radius: 50%;
    cursor: pointer;
    font-size: 1.5rem;
    pointer-events: all;
    transition: all 0.3s;
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.3);
  }

  .carousel-btn:hover {
    background: rgba(255, 255, 255, 0.3);
    transform: scale(1.1);
  }

  .hero-carousel-indicators {
    position: absolute;
    bottom: 3rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 1rem;
    z-index: 3;
  }

  .indicator {
    width: 15px;
    height: 15px;
    border-radius: 50%;
    border: 2px solid white;
    background: transparent;
    cursor: pointer;
    transition: all 0.3s;
  }

  .indicator.active,
  .indicator:hover {
    background: white;
    transform: scale(1.3);
  }

  /* Scroll Indicator */
  .scroll-indicator {
    position: absolute;
    bottom: 2rem;
    right: 3rem;
    text-align: center;
    color: white;
    z-index: 3;
    animation: bounce 2s infinite;
  }

  .scroll-text {
    font-size: 0.9rem;
    margin-bottom: 0.5rem;
    opacity: 0.8;
  }

  .scroll-arrow {
    font-size: 1.5rem;
    animation: bounceArrow 1.5s ease-in-out infinite;
  }

  /* Animated Stats Section */
  .stats-section {
    padding: 5rem 0;
    background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
    position: relative;
    overflow: hidden;
  }

  .stats-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="20" cy="20" r="2" fill="rgba(37,99,235,0.1)"/><circle cx="80" cy="80" r="2" fill="rgba(37,99,235,0.1)"/></svg>');
    opacity: 0.5;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 3rem;
    position: relative;
    z-index: 1;
  }

  .stat-card {
    text-align: center;
    background: white;
    padding: 3rem 2rem;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    transition: all 0.4s ease;
    border: 1px solid rgba(37, 99, 235, 0.1);
    position: relative;
    overflow: hidden;
  }

  .stat-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(37, 99, 235, 0.1), transparent);
    transition: left 0.5s;
  }

  .stat-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 20px 40px rgba(37, 99, 235, 0.2);
  }

  .stat-card:hover::before {
    left: 100%;
  }

  .stat-icon {
    font-size: 3rem;
    margin-bottom: 1.5rem;
    display: block;
  }

  .stat-number {
    font-size: 3.5rem;
    font-weight: 800;
    color: #2563eb;
    margin-bottom: 1rem;
    display: block;
  }

  .stat-text {
    font-size: 1.2rem;
    font-weight: 600;
    color: #1e293b;
    margin-bottom: 0.5rem;
  }

  .stat-description {
    color: #64748b;
    font-size: 0.9rem;
  }

  /* Services Preview */
  .services-preview {
    padding: 6rem 0;
    background: white;
    position: relative;
  }

  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-header h2 {
    font-size: 3rem;
    font-weight: 700;
    color: #1e293b;
    margin-bottom: 1rem;
  }

  .section-header p {
    font-size: 1.2rem;
    color: #64748b;
    max-width: 600px;
    margin: 0 auto;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2.5rem;
  }

  .service-card {
    background: white;
    padding: 2.5rem;
    border-radius: 20px;
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
    transition: all 0.4s ease;
    border: 1px solid #e2e8f0;
    position: relative;
    overflow: hidden;
  }

  .service-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(45deg, #2563eb, #3b82f6);
    transform: scaleX(0);
    transition: transform 0.3s ease;
  }

  .service-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 20px 40px rgba(37, 99, 235, 0.15);
  }

  .service-card:hover::before {
    transform: scaleX(1);
  }

  .service-badge {
    position: absolute;
    top: 1rem;
    right: 1rem;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .service-badge.popular {
    background: linear-gradient(45deg, #f59e0b, #fbbf24);
    color: white;
  }

  .service-badge.nuevo {
    background: linear-gradient(45deg, #10b981, #34d399);
    color: white;
  }

  .service-icon {
    font-size: 4rem;
    margin-bottom: 1.5rem;
    display: block;
  }

  .service-card h3 {
    font-size: 1.5rem;
    font-weight: 700;
    color: #1e293b;
    margin-bottom: 1rem;
  }

  .service-description {
    color: #64748b;
    margin-bottom: 2rem;
    line-height: 1.6;
  }

  .service-features {
    list-style: none;
    margin-bottom: 2rem;
    padding: 0;
  }

  .service-features li {
    padding: 0.5rem 0;
    color: #475569;
    position: relative;
    padding-left: 1.5rem;
  }

  .service-features li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: #22c55e;
    font-weight: bold;
  }

  .service-price {
    font-size: 1.5rem;
    font-weight: 700;
    color: #2563eb;
    margin-bottom: 2rem;
  }

  .service-actions {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .service-cta {
    background: linear-gradient(45deg, #2563eb, #3b82f6);
    color: white;
    border: none;
    padding: 1rem 2rem;
    border-radius: 50px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .service-cta:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(37, 99, 235, 0.4);
  }

  .service-link {
    color: #2563eb;
    text-decoration: none;
    font-weight: 500;
    text-align: center;
    transition: color 0.3s ease;
  }

  .service-link:hover {
    color: #1d4ed8;
  }

  /* Clients Carousel */
  .clients-carousel-section {
    padding: 6rem 0;
    background: linear-gradient(135deg, #1e293b 0%, #475569 100%);
    color: white;
  }

  .clients-carousel-section .section-header h2,
  .clients-carousel-section .section-header p {
    color: white;
  }

  .clients-carousel-container {
    position: relative;
    max-width: 800px;
    margin: 0 auto;
  }

  .clients-carousel {
    position: relative;
    height: 400px;
    border-radius: 20px;
    overflow: hidden;
  }

  .client-slide {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
    transition: opacity 1s ease-in-out;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .client-slide.active {
    opacity: 1;
  }

  .client-content {
    background: rgba(255, 255, 255, 0.1);
    padding: 3rem;
    border-radius: 20px;
    text-align: center;
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    width: 100%;
    max-width: 600px;
  }

  .client-logo {
    width: 80px;
    height: 80px;
    margin: 0 auto 1.5rem;
    border-radius: 50%;
    overflow: hidden;
    background: white;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .client-logo img {
    width: 60px;
    height: 60px;
    object-fit: contain;
  }

  .client-info h3 {
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
  }

  .client-industry {
    color: #fbbf24;
    font-weight: 500;
  }

  .client-project {
    margin: 2rem 0;
  }

  .client-project h4 {
    margin-bottom: 1rem;
    font-size: 1.1rem;
  }

  .client-result {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    background: rgba(34, 197, 94, 0.2);
    padding: 1rem;
    border-radius: 10px;
    border-left: 4px solid #22c55e;
  }

  .result-icon {
    font-size: 1.2rem;
  }

  .result-text {
    font-weight: 600;
  }

  .client-testimonial {
    font-style: italic;
    font-size: 1.1rem;
    margin: 2rem 0;
    line-height: 1.6;
    opacity: 0.9;
  }

  .case-study-btn {
    margin-top: 1rem;
  }

  .clients-carousel-controls {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 100%;
    display: flex;
    justify-content: space-between;
    padding: 0 2rem;
    pointer-events: none;
  }

  .clients-carousel-controls .carousel-btn {
    pointer-events: all;
  }

  /* Technology Section */
  .technology-section {
    padding: 6rem 0;
    background: #f8fafc;
  }

  .tech-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
  }

  .tech-category {
    background: white;
    padding: 2rem;
    border-radius: 15px;
    text-align: center;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
    transition: all 0.3s ease;
    cursor: pointer;
  }

  .tech-category:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 25px rgba(37, 99, 235, 0.15);
  }

  .tech-category h3 {
    color: #1e293b;
    margin-bottom: 1.5rem;
    font-size: 1.3rem;
  }

  .tech-icons {
    display: flex;
    justify-content: center;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .tech-icon {
    width: 3rem;
    height: 3rem;
    border-radius: 50%;
    background: linear-gradient(45deg, #2563eb, #3b82f6);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    cursor: pointer;
    transition: all 0.3s ease;
    position: relative;
  }

  .tech-icon:hover {
    transform: scale(1.2) rotate(10deg);
    box-shadow: 0 4px 15px rgba(37, 99, 235, 0.4);
  }

  .tech-icon::after {
    content: attr(data-tech);
    position: absolute;
    bottom: -2rem;
    left: 50%;
    transform: translateX(-50%);
    font-size: 0.7rem;
    color: #64748b;
    opacity: 0;
    transition: opacity 0.3s ease;
    white-space: nowrap;
  }

  .tech-icon:hover::after {
    opacity: 1;
  }

  /* CTA Section */
  .cta-section {
    padding: 6rem 0;
    background: linear-gradient(135deg, #2563eb 0%, #3b82f6 100%);
    color: white;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .cta-background {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="20" cy="20" r="3" fill="rgba(255,255,255,0.1)"/><circle cx="80" cy="80" r="3" fill="rgba(255,255,255,0.1)"/><circle cx="40" cy="60" r="2" fill="rgba(255,255,255,0.1)"/></svg>');
    animation: backgroundMove 20s linear infinite;
  }

  .cta-content {
    position: relative;
    z-index: 1;
  }

  .cta-content h2 {
    font-size: 3rem;
    font-weight: 800;
    margin-bottom: 1.5rem;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
  }

  .cta-content p {
    font-size: 1.3rem;
    margin-bottom: 3rem;
    opacity: 0.95;
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
  }

  .cta-stats {
    display: flex;
    justify-content: center;
    gap: 3rem;
    margin-bottom: 3rem;
    flex-wrap: wrap;
  }

  .cta-stat {
    text-align: center;
  }

  .cta-stat-number {
    display: block;
    font-size: 2rem;
    font-weight: 800;
    color: #fbbf24;
    margin-bottom: 0.5rem;
  }

  .cta-stat-text {
    font-size: 0.9rem;
    opacity: 0.9;
  }

  .cta-buttons {
    display: flex;
    gap: 2rem;
    justify-content: center;
    margin-bottom: 3rem;
    flex-wrap: wrap;
  }

  .cta-primary {
    background: #1e293b;
    font-size: 1.2rem;
    padding: 1.5rem 3rem;
  }

  .cta-primary:hover {
    background: #0f172a;
    transform: translateY(-3px);
    box-shadow: 0 10px 30px rgba(30, 41, 59, 0.4);
  }

  .cta-secondary {
    background: transparent;
    border: 2px solid white;
    font-size: 1.1rem;
    padding: 1.4rem 2.8rem;
  }

  .cta-footer {
    opacity: 0.8;
    font-size: 0.9rem;
  }

  /* Video Modal */
  .video-modal {
    display: none;
    position: fixed;
    z-index: 2000;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.9);
  }

  .video-modal-content {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 90%;
    max-width: 800px;
    background: #1e293b;
    border-radius: 15px;
    overflow: hidden;
  }

  .video-modal-close {
    color: white;
    position: absolute;
    top: 1rem;
    right: 1rem;
    font-size: 2rem;
    font-weight: bold;
    cursor: pointer;
    z-index: 2001;
    background: rgba(0,0,0,0.5);
    width: 3rem;
    height: 3rem;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .video-modal-close:hover {
    background: rgba(0,0,0,0.8);
  }

  .video-container {
    position: relative;
    width: 100%;
    height: 0;
    padding-bottom: 56.25%; /* 16:9 aspect ratio */
  }

  .video-container iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
  }

  /* Animations */
  @keyframes heroSlideIn {
    from {
      opacity: 0;
      transform: translateY(50px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-10px);
    }
    60% {
      transform: translateY(-5px);
    }
  }

  @keyframes bounceArrow {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(10px);
    }
  }

  @keyframes backgroundMove {
    0% {
      transform: translateX(0) translateY(0);
    }
    100% {
      transform: translateX(-100px) translateY(-100px);
    }
  }

  @keyframes countUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .hero-title {
      font-size: 2.5rem;
    }

    .hero-subtitle {
      font-size: 1.2rem;
    }

    .hero-description {
      font-size: 1rem;
    }

    .hero-stats {
      flex-direction: column;
      gap: 1rem;
    }

    .hero-actions {
      flex-direction: column;
      align-items: stretch;
    }

    .hero-carousel-controls {
      padding: 0 1rem;
    }

    .carousel-btn {
      width: 3rem;
      height: 3rem;
      font-size: 1.2rem;
    }

    .scroll-indicator {
      display: none;
    }

    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 2rem;
    }

    .stat-card {
      padding: 2rem 1rem;
    }

    .services-grid {
      grid-template-columns: 1fr;
    }

    .section-header h2 {
      font-size: 2rem;
    }

    .cta-content h2 {
      font-size: 2rem;
    }

    .cta-stats {
      flex-direction: column;
      gap: 1rem;
    }

    .cta-buttons {
      flex-direction: column;
      align-items: center;
    }

    .tech-grid {
      grid-template-columns: 1fr;
    }

    .container {
      padding: 0 1rem;
    }
  }

  @media (max-width: 480px) {
    .hero-title {
      font-size: 2rem;
    }

    .stats-grid {
      grid-template-columns: 1fr;
    }

    .hero-stat-item {
      font-size: 0.9rem;
    }

    .client-content {
      padding: 2rem 1.5rem;
    }
  }

  /* Scroll animations */
  .fade-in-up {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.6s ease;
  }

  .fade-in-up.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Add placeholder styling for missing images */
  .hero-background img,
  .client-logo img {
    background: linear-gradient(135deg, #f1f5f9 0%, #e2e8f0 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    color: #94a3b8;
    font-size: 3rem;
  }

  .hero-background img::before {
    content: "🏢";
  }

  .client-logo img::before {
    content: "🏭";
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    // Hero Carousel
    const heroCarousel = document.getElementById('heroCarousel');
    const heroSlides = heroCarousel.querySelectorAll('.hero-slide');
    const heroPrevBtn = document.getElementById('heroPrevBtn');
    const heroNextBtn = document.getElementById('heroNextBtn');
    const heroIndicators = document.querySelectorAll('.hero-carousel-indicators .indicator');
    
    let currentHeroSlide = 0;

    function showHeroSlide(index) {
      heroSlides.forEach(slide => slide.classList.remove('active'));
      heroIndicators.forEach(indicator => indicator.classList.remove('active'));
      
      heroSlides[index].classList.add('active');
      heroIndicators[index].classList.add('active');
      currentHeroSlide = index;
    }

    function nextHeroSlide() {
      const next = (currentHeroSlide + 1) % heroSlides.length;
      showHeroSlide(next);
    }

    function prevHeroSlide() {
      const prev = (currentHeroSlide - 1 + heroSlides.length) % heroSlides.length;
      showHeroSlide(prev);
    }

    // Hero carousel controls
    heroNextBtn.addEventListener('click', nextHeroSlide);
    heroPrevBtn.addEventListener('click', prevHeroSlide);

    heroIndicators.forEach((indicator, index) => {
      indicator.addEventListener('click', () => showHeroSlide(index));
    });

    // Auto-play hero carousel
    setInterval(nextHeroSlide, 8000);

    // Clients Carousel
    const clientsCarousel = document.getElementById('clientsCarousel');
    const clientSlides = clientsCarousel.querySelectorAll('.client-slide');
    const clientsPrevBtn = document.getElementById('clientsPrevBtn');
    const clientsNextBtn = document.getElementById('clientsNextBtn');
    
    let currentClientSlide = 0;

    function showClientSlide(index) {
      clientSlides.forEach(slide => slide.classList.remove('active'));
      clientSlides[index].classList.add('active');
      currentClientSlide = index;
    }

    function nextClientSlide() {
      const next = (currentClientSlide + 1) % clientSlides.length;
      showClientSlide(next);
    }

    function prevClientSlide() {
      const prev = (currentClientSlide - 1 + clientSlides.length) % clientSlides.length;
      showClientSlide(prev);
    }

    // Clients carousel controls
    clientsNextBtn.addEventListener('click', nextClientSlide);
    clientsPrevBtn.addEventListener('click', prevClientSlide);

    // Auto-play clients carousel
    setInterval(nextClientSlide, 6000);

    // Animated counters for stats
    const animateCounter = (element, target) => {
      const duration = 2000;
      const start = 0;
      const increment = target / (duration / 16);
      let current = start;

      const timer = setInterval(() => {
        current += increment;
        if (current >= target) {
          current = target;
          clearInterval(timer);
        }
        
        if (target.toString().includes('%')) {
          element.textContent = Math.floor(current) + '%';
        } else if (target.toString().includes('+')) {
          element.textContent = Math.floor(current) + '+';
        } else {
          element.textContent = Math.floor(current);
        }
      }, 16);
    };

    // Intersection Observer for animations
    const observerOptions = {
      threshold: 0.2,
      rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          
          // Animate stat numbers
          if (entry.target.classList.contains('stat-card')) {
            const numberElement = entry.target.querySelector('.stat-number');
            const targetValue = numberElement.getAttribute('data-target');
            if (targetValue && !numberElement.classList.contains('animated')) {
              numberElement.classList.add('animated');
              animateCounter(numberElement, parseInt(targetValue));
            }
          }
        }
      });
    }, observerOptions);

    // Observe elements for animation
    const animatedElements = document.querySelectorAll('.stat-card, .service-card, .tech-category');
    animatedElements.forEach(el => {
      el.classList.add('fade-in-up');
      observer.observe(el);
    });

    // Service CTA buttons
    const serviceCtas = document.querySelectorAll('.service-cta');
    serviceCtas.forEach(button => {
      button.addEventListener('click', () => {
        const serviceName = button.getAttribute('data-service');
        window.location.href = `/contacto?servicio=${encodeURIComponent(serviceName)}`;
      });
    });

    // Case study buttons
    const caseStudyBtns = document.querySelectorAll('.case-study-btn');
    caseStudyBtns.forEach(button => {
      button.addEventListener('click', () => {
        const clientName = button.getAttribute('data-client');
        window.location.href = `/clientes?cliente=${encodeURIComponent(clientName)}`;
      });
    });

    // Video modal functionality
    const videoModal = document.getElementById('videoModal');
    const videoModalClose = document.querySelector('.video-modal-close');
    const playVideoButtons = document.querySelectorAll('.play-video');
    const videoFrame = document.getElementById('videoFrame');

    playVideoButtons.forEach(button => {
      button.addEventListener('click', () => {
        const videoType = button.getAttribute('data-video');
        let videoUrl = '';
        
        switch(videoType) {
          case 'company-intro':
            videoUrl = 'https://www.youtube.com/embed/dQw4w9WgXcQ?autoplay=1';
            break;
          default:
            videoUrl = 'https://www.youtube.com/embed/dQw4w9WgXcQ?autoplay=1';
        }
        
        videoFrame.src = videoUrl;
        videoModal.style.display = 'block';
        document.body.style.overflow = 'hidden';
      });
    });

    videoModalClose.addEventListener('click', () => {
      videoModal.style.display = 'none';
      videoFrame.src = '';
      document.body.style.overflow = 'auto';
    });

    window.addEventListener('click', (e) => {
      if (e.target === videoModal) {
        videoModal.style.display = 'none';
        videoFrame.src = '';
        document.body.style.overflow = 'auto';
      }
    });

    // Tech icons hover effects
    const techIcons = document.querySelectorAll('.tech-icon');
    techIcons.forEach(icon => {
      icon.addEventListener('mouseenter', () => {
        icon.style.transform = 'scale(1.2) rotate(10deg)';
      });
      
      icon.addEventListener('mouseleave', () => {
        icon.style.transform = 'scale(1) rotate(0deg)';
      });
    });

    // Smooth scrolling for internal links
    const scrollLinks = document.querySelectorAll('a[href^="#"]');
    scrollLinks.forEach(link => {
      link.addEventListener('click', (e) => {
        e.preventDefault();
        const targetId = link.getAttribute('href').substring(1);
        const targetElement = document.getElementById(targetId);
        
        if (targetElement) {
          const headerHeight = document.querySelector('.header').offsetHeight;
          const targetPosition = targetElement.offsetTop - headerHeight;
          
          window.scrollTo({
            top: targetPosition,
            behavior: 'smooth'
          });
        }
      });
    });

    // Keyboard navigation for carousels
    document.addEventListener('keydown', (e) => {
      if (e.target.tagName !== 'INPUT' && e.target.tagName !== 'TEXTAREA') {
        if (e.key === 'ArrowLeft') {
          prevHeroSlide();
        } else if (e.key === 'ArrowRight') {
          nextHeroSlide();
        }
      }
    });

    // Parallax effect for floating elements
    window.addEventListener('scroll', () => {
      const scrolled = window.pageYOffset;
      const parallax = document.querySelectorAll('.floating-circle');
      const speed = 0.5;

      parallax.forEach(element => {
        const yPos = -(scrolled * speed);
        element.style.transform = `translateY(${yPos}px)`;
      });
    });
  });
</script>